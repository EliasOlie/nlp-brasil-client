import styled from "styled-components";
import COLORS from "./Colors";

export const H2WithStyle = styled.h2 `
    position: relative;
    top: 10em;
    
    display: flex;
    align-items: center;
    justify-content: center;

    color: #${COLORS.YELLOW};
    text-align: center;

`;